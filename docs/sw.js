const CACHE='mahjong-link-pwa-v7';
const TILES=Array.from({length:27},(_,i)=>`./tiles/tile-real-${i}.png?v=10`);
const CORE=['./','./index.html','./compact.css?v=15','./font-fixes.css?v=15','./board-10x10.css?v=15','./theme-v4.css?v=15','./overlays-v6.css?v=15','./pwa-v9.css?v=15','./ui-overlays.js?v=15','./cache-progress.js?v=15','./manifest.webmanifest','./mahjong-serif.woff2','./icons/icon-180.png','./icons/icon-192.png','./icons/icon-512.png',...TILES];
const report=(done,total,complete=false)=>self.clients.matchAll({includeUncontrolled:true,type:'window'}).then(clients=>clients.forEach(client=>client.postMessage({type:'CACHE_PROGRESS',done,total,complete})));
const download=async(name=CACHE)=>{const cache=await caches.open(name);let done=0;for(const asset of CORE){await cache.add(asset);done++;await report(done,CORE.length,done===CORE.length)}};
const refresh=async()=>{const temp=CACHE+'-refresh';await caches.delete(temp);await download(temp);const source=await caches.open(temp);const requests=await source.keys();await caches.delete(CACHE);const target=await caches.open(CACHE);for(const request of requests)await target.put(request,await source.match(request));await caches.delete(temp);await report(CORE.length,CORE.length,true)};
self.addEventListener('install',event=>event.waitUntil(download().then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil((async()=>{const keys=await caches.keys();await Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)));await self.clients.claim();await report(CORE.length,CORE.length,true)})()));
self.addEventListener('message',event=>{if(event.data?.type==='GET_CACHE_PROGRESS')event.waitUntil(caches.open(CACHE).then(cache=>cache.keys()).then(keys=>report(Math.min(keys.length,CORE.length),CORE.length,keys.length>=CORE.length)));if(event.data?.type==='FORCE_CACHE_UPDATE')event.waitUntil(refresh())});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(url.origin!==location.origin)return;if(event.request.mode==='navigate'){event.respondWith(fetch(event.request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put('./index.html',copy));return response}).catch(()=>caches.match('./index.html')));return}event.respondWith(caches.match(event.request).then(cached=>{const fresh=fetch(event.request).then(response=>{if(response.ok)caches.open(CACHE).then(cache=>cache.put(event.request,response.clone()));return response}).catch(()=>cached);return cached||fresh}))});
